apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-outbox-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-outbox-processor
  template:
    metadata:
      labels:
        app: catalog-outbox-processor
    spec:
      serviceAccountName: fcg-outbox-processor
      containers:
        - name: outbox-processor
          image: ghcr.io/posgraduacaofiapnet/fcg-outbox-processor:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: outbox-processor-config
          env:
            - name: Outbox__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: catalog-api-secrets
                  key: CatalogConnectionString
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 250m
              memory: 256Mi
